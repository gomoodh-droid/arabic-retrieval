# بروتوكول المساهمة بالقياسات والمتون · Data Contribution Protocol

أنفع مساهمة للمشروع هي **تقديم قياسات على متون عربية جديدة** (فقهية، قانونية، طبية، عامية، تراثية).

---

## 📋 صيغة المتن المطلوبة

أنشئ ملف البيانات بالصيغة التالية:

```json
{
  "passages": [
    {"id": "d1", "text": "نص المستند العربي..."}
  ],
  "questions": [
    {"id": "q1", "q": "سؤال باللغة العربية...", "gold": ["d1"]}
  ]
}
```

---

## 🧪 تشغيل الضبط وإرسال النتيجة

```bash
python tune.py my_data.json --questions my_questions.json --out best_config.json
```

ثم قُم بتقديم ملف `best_config.json` وتقرير النتائج لإضافته إلى جداول القياس الموثقة بالملف `RESULTS.md`.
